# Atlas Visibility System - Production Configuration
# Retention policy (default: 90 days)
ATLAS_RETENTION_DAYS=90

# Control plane (optional)
ATLAS_URL=http://localhost:8080
ATLAS_API_TOKEN=dev-token-archie

# Database location (default: ~/.config/atlas)
# XDG_CONFIG_HOME=$HOME/.config
